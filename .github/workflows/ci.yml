name: CI Pipeline

on:
  push:
    branches: [ main, dev-01, "dev-frontend" ]
  pull_request:
    branches: [ main, dev-01 ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Sanity check (import Flask app)
        run: |
          python -c "import sys; sys.path.insert(0, 'src/backend'); import app; print('OK')"